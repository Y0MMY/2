<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:launher.Templates"
                    x:Class="launher.Templates.ButtonAnimate"
                    >

    

    <ControlTemplate x:Key="button" TargetType="Button" >
        <Grid MouseEnter="Button_MouseEnter" MouseLeave="Button_MouseLeave"
                 Width="{TemplateBinding Width}" Height="{TemplateBinding Height}">
            <Rectangle x:Name="mainBack" RadiusX="5" RadiusY="5" Fill="{TemplateBinding Background}"
                    >
             
            </Rectangle>
            <ContentPresenter Margin="{TemplateBinding Padding}" Content="{TemplateBinding Content}"
                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </ControlTemplate>

</ResourceDictionary>